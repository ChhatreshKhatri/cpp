#include<bits/stdc++.h>
using namespace std;
//https://youtu.be/0N3RCWa8jFM

/*vector<vector<int>> recurpwrset(vector<int>&v,int i){
    if(i>=v.size()) return {{}};
    vector<vector<int>> partialAns=recurpwrset(v,i+1);
    vector<vector<int>> ans;
    for(vector<int> x:partialAns)ans.push_back(x);
    for(vector<int> x:partialAns){
        x.push_back(v[i]);
        ans.push_back[x];
    }
    return ans;
}
vector<vector<int>> pwset(vector<int>v)
{
    return recurpwrset(v,0);
}*/ 

int main()
{
    

    return 0;
}